{
  "name": "api-proxy-server",
  "version": "1.0.0",
  "description": "Proxy server for AI APIs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.7",
    "express-rate-limit": "^7.1.3",
    "helmet": "^7.1.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
